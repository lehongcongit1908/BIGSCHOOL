@{
    ViewBag.Title = "Home Page";
}


<p>
    @Html.ActionLink("Create New", "Create","Courses_2")
</p>

@using BIGSCHOOL.Models;
@{ ViewBag.Title = "Home Page"; }

<div class="row">
    <ul>
        @foreach (var course in Model)
        {
            <li>
                @course.DateTime.ToString("dd/MM/yyyy HH:mm") -@course.Name
            </li>
        }
    </ul>

</div>