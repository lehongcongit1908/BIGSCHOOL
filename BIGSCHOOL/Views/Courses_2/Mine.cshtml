@model IEnumerable<BIGSCHOOL.Models.Course>

@{
    ViewBag.Title = "Mine";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Khóa học của tôi</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<div class="row">
    <ul class="course">
        @foreach (var course in Model)
        {
            <li> @course.LectureName == @course.Category.Name</li>
            @*<li>
                <div class="date">
                    <div class="month">
                        @course.DateTime.ToString("MM")
                    </div>

                    <div class="day">

                        @course.DateTime.ToString("dd")
                    </div>

                    <div class="detail">
                        <span class="lecture">
                            @course.LectureName

                        </span>
                        <br />

                        <span class="category">
                            @course.Category.Name

                        </span>



                    </div>
                    <div class="action">
                        <a href="#"> Edit </a>
                        <a href="#"> Delete </a>

                    </div>

                </div>
            </li>*@

        }

    </ul>

</div>

